# Automation Log

- Append-only file: `changes.jsonl` — one JSON object per batch.
- Optional human-readable: `latest.md`
- Used by Codex, Claude Code, and others to sync context and avoid stepping on each other.
