[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "redna-core-demo"
version = "0.1.0"
description = "Demo of the ReDNA Core system"
authors = [
    { name="Your Name", email="you@example.com" }
]

[tool.setuptools]
packages = ["core"]