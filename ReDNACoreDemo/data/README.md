This directory stores runtime data, user telemetry, and other mutable artifacts generated by ReDNA core services. The files are ignored in Git via the root `.gitignore` to keep the repository lightweight and protect user data. Commit only seed datasets or documentation from this folder.
